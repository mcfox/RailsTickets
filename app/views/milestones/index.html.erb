<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", milestones_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_milestone_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> Milestones</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.milestone.project_id", :default => t("activerecord.labels.project_id", :default => "Project_id")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @milestones.each do |milestone| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= milestone.id %>
          </td>
                    <td>
            <%= link_to milestone.project_id, milestone_path(milestone) %>
          </td>
                    <td>
            <%= milestone.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", milestone_path(milestone) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_milestone_path(milestone) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", milestone_path(milestone), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
